language: python
python:
    - "2.7"
    - "3.3"
    - "3.4"
    - "3.5"

# install python dependencies including this package in the travis
# virtualenv
install:
  - ./provision/python.sh

script:
    - pep8 mediawiki/
    - python setup.py test
